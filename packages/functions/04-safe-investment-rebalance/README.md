# Safe investment rebalance task

It's an event triggere tasks that it's executed every time one of this events `RedeemMorpho(uint256)` |`RedeemCompound(uint256)` | `RedeemAave(uint256)` its logged and it gets from the subgraph which of the protocols it's giving best APY and sends the funds there
